# Python bytecode / caches
__pycache__/
*.py[cod]

# Packaging / build artifacts
*.egg-info/
build/
dist/

# Test / type-check caches
.pytest_cache/
.hypothesis/
.mypy_cache/
.ruff_cache/

# Local virtual environments
.venv/
venv/

# macOS
.DS_Store

# AWS SAM
# samconfig.toml holds per-deployer config (stack name, region, S3 bucket,
# parameter overrides) — kept local, out of the repo.
samconfig.toml
.aws-sam/

# Reference docs (local only, not shipped)
_reference_docs/

# Kiro IDE config (local only)
.kiro/
examples/s3-files-data-analyst/microvm-image/worker/node_modules/
